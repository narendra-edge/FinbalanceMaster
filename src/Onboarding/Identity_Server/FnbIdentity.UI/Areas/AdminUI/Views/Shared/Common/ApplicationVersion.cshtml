@using System.Reflection

@using FnbIdentity.UI.Helpers

@{
    var runtimeVersion = typeof(StartupHelpers)
        .GetTypeInfo()
        .Assembly
        .GetCustomAttribute<AssemblyInformationalVersionAttribute>()?.InformationalVersion;
}

@runtimeVersion